# CLAUDE.md — BioVid熱疼痛強度推定プロジェクト

> このファイルはClaude Codeがセッション開始時に自動で読み込む、プロジェクトの永続的な文脈です。
> 情報源: Antigravityによるリポジトリ実地調査(コード読解・コマンド実行済み)をベースに、ユーザー本人による訂正・追記(2026年9月)を反映済みです。
> `⚠️ 要確認` の付いた項目は、まだユーザーとのすり合わせが未完了です。断定せず、扱いに迷ったら質問してください。

---

## 1. 研究の目的・仮説

- **最終目標(プロジェクト全体)**: 投薬麻酔の負担軽減のための**電子麻酔技術**の確立。
- **現在の位置づけ**: 学士4年の卒業研究として、その電子麻酔プロジェクトの最初のステップである「疼痛推定」にフォーカスしている段階。このまま進めば疼痛推定自体が卒業研究テーマになる見込み。
- **研究課題**: BioVid Heat Pain Database を用い、装着負担の少ない自律神経信号(EDA・ECG)のみから急性熱疼痛強度を推定するモデルを構築する。
  - 5クラス分類: T0(無痛)〜T4(最大熱痛)
  - 2クラス分類: T0 vs T4
- **中心的な課題意識**: 被験者間バイアスと実験プロトコルの順序固定に由来する「暗記リーク」を排除し、未知の被験者に真に汎化する性能を証明すること。
- **仮説**: 5.5秒の短時間窓では統計量(RMSSD、LF/HF等)が不安定になるが、安静時からの相対変化率(ΔRRI)と直前試行の時間文脈(移動平均・差分)を加え、超軽量Transformerで被験者内Self-Attentionを行うことで、過学習なく高精度な分類が可能になる。
- **EMGを使わない理由(重要)**: 単に未検討なのではなく、**電子麻酔技術という最終ゴールを見据え、初期段階では麻酔と併用される計測を想定しているため**。麻酔下では表情筋の動きに基づくEMG計測がほぼ想定できないことから、現段階では意図的に見送っている(6章も参照)。

---

## 2. 現在の到達状況

- 現状は**疼痛推定モデルの精度向上**に取り組んでいる段階。研究としての次のステップ(卒論テーマとして何を軸にするか等)はまだ検討中。
- **週1回、指導教授と進捗を確認している。**
- **⚠️ Transformerの結果には既に疑念が出ている。** 精度の数値そのものだけでなく、**実験方法・使用データの両方について要検証**の状態。以下の数値は参考値であり、確定した最終結果として扱わないこと。再検証が終わるまでは、この結果を前提に新しい実装を積み上げることは避け、まず検証作業を優先する。
- **これまでの実験結果(要再確認の前提で記載)**:
  - 2クラス分類(T0 vs T4): 自前12次元特徴量 + Random Forest **69.55%**、C4構成 + 軽量Transformer **85.74%**
  - 5クラス分類(T0〜T4): C4構成 + 軽量Transformer **36.31%**(Macro F1 ≈ 0.358)
  - 順序暗記リーク・被験者リーク・過剰フィルタリング問題について、数理的・実験的な検証を実施済み
- **実装済みファイル**:
  - `src/preprocess_pipeline.py` — HDF5前処理パイプライン
  - `src/reextract_features_nk.py` — NeuroKit2による特徴量再抽出
  - `src/feature_extraction.py` — 特徴量抽出エンジン
  - `src/train_eval.py` — LOSO評価エンジン(Random Forest / SVM RBF)
  - `src/train_eval_transformer.py` — 軽量Transformer訓練・評価
  - `src/reproduce_ddcae.py` — 先行研究DDCAE再現・過学習検証
  - 全8ステージの検証・比較グラフ自動生成スクリプト群

---

## 3. 運用ルール

- **`data/` 配下への書き込みは絶対禁止。** これは確定ルール。**✅ OSレベルのACLで技術的に強制済み(2026年9月確認)。**
  - `data/` は `G:\dataset`(実データ本体、148GB)へのジャンクション。
  - `G:\dataset` に対して、NTFSの拒否ACLを設定済み: 対象ユーザーに対し `書き込み(WD, AD, WEA, WA)` `削除(DE)` `子の削除(DC)` を拒否。設定はエクスプローラーの「プロパティ→セキュリティ→詳細設定」から実施し、既存ファイル・サブフォルダ全体(26,761個)に伝播させた。
  - 拒否はNTFS上で許可より常に優先されるため、Full Control権限が残っていても書き込み・削除は実際にブロックされる(`echo test > "G:\dataset\test.txt"` で「アクセスが拒否されました」を実地確認済み)。
  - `BUILTIN\Administrators` と `NT AUTHORITY\SYSTEM` には拒否をかけていないため、復旧作業が必要な場合は管理者権限で対応可能。
  - **再現・復元コマンド(参考)**:
    ```
    # 拒否を追加する場合(GUIでも同等の設定が可能)
    icacls "G:\dataset" /grant "%USERNAME%":(OI)(CI)F /T
    icacls "G:\dataset" /deny "%USERNAME%":(OI)(CI)(W,DE,DC) /T
    # 解除する場合
    icacls "G:\dataset" /remove:d "%USERNAME%" /T
    ```
  - 補助的な二重防御として、Claude Code側の `.claude/settings.json` にも `"deny": ["Edit(data/**)"]` を設定すること(こちらは既知の反映漏れバグ報告があるため、OSレベルACLを本丸として扱う)。
- **LOSO(Leave-One-Subject-Out)について**: 「必ずLOSOを使う」という厳格なルールとして明言されたわけではないが、採用している理由(被験者データが訓練/テストに混ざることによるデータリーク防止)自体は妥当と考えられている。現時点でのデフォルトの評価手法として扱ってよいが、絶対不変のルールとまでは断定しないこと。
- **実験のやり直しについて**: 同一条件での再実験は問題ない。コードレビューの結果次第では、既存の実験を同条件で再度行う可能性もある。「既存結果を壊さないように」と過度に慎重になる必要はない。

---

## 4. データ仕様

- **主に使用してきたデータ**: BioVid Heat Pain Database **Part B**
  - 被験者数: 84名(公式Excel上は85名だが、信号Zip・サンプルCSVとのアライメントが取れる84名に限定)
  - 試行構成: 1被験者あたり100試行(5クラス各20試行)。2クラス時はT0 20試行 + T4 20試行(計40試行)。
  - 窓長・サンプリングレート: 5.5秒 / 512Hz(2,817サンプル/試行)
- **今後の拡張方針**:
  - **Part C(窓長15秒)も今後使用していく方針。**
  - フィルタを使わない前処理方針を取る場合は **Part A** も検討対象。
  - データセットの詳細説明はObsidian側にノートがある可能性が高く、必要に応じて共有予定。
- **信号**: EDA(`gsr`)、ECG(`ecg`)、時間軸(`time`)。EMGは1章の理由により現状不使用。
- **ラベル**: `class_id`(0=T0〜4=T4)

### 前処理方針(Part Bベースの現行版)
- BioVid公式の前処理済み信号(`biosignals_filtered`)を使用し、追加の二重フィルタリングは行わない。
- NeuroKit2/SciPyでEDAをSCL(Tonic)とSCR(Phasic)に分離、ECGからRRIを算出。

### 処理済みファイルのスキーマ (`biovid_features.h5` / `biovid_features_nk.h5`)
- 形式: HDF5(`h5py`、Gzip `compression_opts=4`)
- `/raw_signals/{subject_name}/{sample_name}/`
  - `eda`, `ecg`, `time`(いずれもfloat64 1D array)
  - 属性: `subject_id`, `subject_name`, `class_id`, `class_name`, `sample_id`, `sample_name`
- `/tabular_features/`(全8,400行)
  - EDA系: `scr_amplitude`, `rise_time`, `scl_mean`, `scl_std`(または `scr_phasic_mean_nk` 等のNeuroKit2版)
  - ECG系: `mean_rri`, `sdnn`, `rmssd`, `sd1`, `sd2`, `lf`, `hf`, `lf_hf_ratio`
- `/metadata/`: `subject_id`, `subject_name`, `class_id`, `class_name`, `sample_id`, `sample_name`

### 特徴量構成(段階的拡張、Part Bでの実験実績)
- `C1`(12次元): 基本生理特徴量(EDA4 + ECG8)
- `C2`(13次元): C1 + ΔRRI(安静時T0からの相対心拍変化率)
- `C3`(36次元): C1 + 直前3試行の移動平均(`rolling3`) + 前試行差分(`diff`)
- `C4`(39次元): C3 + ΔRRI(とそのrolling3/diff)

---

## 5. モデル・技術選定について

**現時点はまだ「これで行く」という方針決定の段階ではない。** 先行研究を参照しながら、より精度が出そうな手法を幅広く実験している最中であり、以下は「これまで試した内容と結果」であって「今後もこれに限定する」という意味ではない。

- **軽量Transformer(`LightweightTransformer`)**: `embed_dim=16, num_heads=2, num_layers=1, dim_feedforward=32, dropout=0.1`。次元を極小化し、被験者内試行のSelf-Attentionで相対評価させることで過学習を回避する狙い。
- **`joblib.Parallel`によるLOSO並列化**: 84被験者ループをマルチコア処理し計算時間を短縮。
- **参考: 試した上で精度が出なかった/不採用にした手法**
  - 生波形直接入力の深層1D-CNN(DDCAE再現): LOSO評価で13.33%まで精度崩壊
  - 5.5秒窓単体でのFFT/Welch LF-HF解析: 分解能限界(0.18Hz)によりNaN化

新しい手法を提案する際は、「これが最終方針である」という前提を置かず、他の選択肢と並列に試す一案として提示すること。

### ⚠️ Transformerに関する要検証事項(最優先の懸念)

- 現在報告されているTransformerの精度(85.74% / 36.31%)には**既に疑念が出ている**。以下の観点で再検証が必要:
  - **実験方法**: 評価プロトコル、データ分割、前処理の適用順序などにリークや誤りがないか
  - **使用データ**: 学習・評価に使ったデータの範囲やバージョンが意図した通りか
- 検証が終わるまでは、この数値を「正しい実績」として次の実装の前提に置かないこと。疑わしい箇所を見つけたら都度報告する。

### 学習support方針(重要)

- **ユーザー自身はTransformer(特にSelf-Attentionの仕組み)をまだ完全には理解していない。** そのため、Transformer関連の作業(実装・デバッグ・再検証いずれも)を進める際は、コードを書いて終わりにせず、**都度「何をしているか・なぜそうなっているか」を解説すること**。
  - 例: 「なぜこのAttentionの計算がこの精度に繋がるのか」「この検証で何が明らかになるのか」を、コード変更と合わせて説明する。
  - 実装と学習を並行して進める前提で対応し、一方的にブラックボックスのまま実装を進めないこと。

---

## 6. 要確認・未確定事項

- **EMG**: 1章の通り基本的には不採用の方針。ただし、**卒論・研究の制度上の要件で採用が必要になった場合は方針変更の可能性がある。** 決め打ちで実装を進めないこと。
- **環境構築ツール(`uv`)**: 使用していない可能性が高い(標準の`venv`と思われる)。断定はしない。
- **モバイル環境(iOS / Working Copy)からのGit同期**: 見送りで確定。今後もこの判断を前提としてよい。
- **バックアップ方針**: 手動でiCloud上の別フォルダにコピーする運用を取っている。自動化されたバックアップ体制ではない点に留意。

---

## 7. 環境情報

- **Python**: 3.12.10
- **仮想環境**: ワークスペース直下 `.venv`(`.\.venv\Scripts\python.exe`)
- **主要ライブラリ(実測バージョン)**:
  - `torch` 2.13.0+cpu
  - `neurokit2` 0.2.13
  - `scipy` 1.18.0
  - `scikit-learn` 1.9.0
  - `h5py` 3.16.0
  - `pandas` 2.3.3
  - `numpy` 2.5.0
  - `joblib` 1.5.3
  - `matplotlib`, `seaborn`

---

## 8. フォルダ構成・Git管理ルール・バックアップ

- **Workspace Root**: `G:\My_Research_Scratch(AI)`
- `src/` — Pythonソースコード(Git管理対象)
- `obsidian_lab/` — ゼミ資料・構想メモ・PDF論文(別Git管理 / `.gitignore`対象)
- `data/` — `G:\dataset` へのジャンクション。生データZip・HDF5。NTFS ACLにより書き込み・削除を拒否済み(3章参照)。
- `brain/` — AIエージェントの生成ログ・可視化出力画像(会話ログはJSON形式)
- **バックアップ**: 手動でiCloud上の別フォルダにコピー。自動同期ではないため、更新の取りこぼしに注意。

---

## 9. 次に着手すべきタスク

- **【最優先】Transformerの実験方法・使用データの再検証。** 疑念が出ている箇所を洗い出し、原因を特定する。この検証と並行して、ユーザーへの仕組みの解説も行う(5章参照)。
- 研究としての次のステップ(卒論テーマの軸をどこに置くか含む)は検討中。具体的なタスクは今後のすり合わせで確定させる。
- 教授との週次ミーティングでのフィードバックを反映。
- Part C(15秒窓)、および無フィルタ方針の場合のPart A利用の検討。
- ハイパーパラメータ感度分析・アブレーション実験の自動化(候補)。
- スクリプト群の共通処理のモジュール整理(候補)。
- **卒論本文の執筆はまだ着手しておらず、かなり先のタスク。** 直近の優先事項ではない。
